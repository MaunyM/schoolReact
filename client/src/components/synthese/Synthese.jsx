import React from 'react'
import {connect} from 'react-redux'

const SchoolSynthese = () => (
    <div>Synthese</div>
);

export default connect(
    (state, ownProps) => ({
    }),
    dispatch => ({
    })
)(SchoolSynthese);